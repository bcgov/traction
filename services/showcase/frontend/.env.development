# development environment env settings
# vue cli will run with env=development by default when running vue-cli-service serve (npm run serve)
# these values can be overridden locally by creating .env.development.local file (which will not commit to git)

VUE_APP_TITLE=Traction Showcase - DEVELOPMENT

# API URL, this defaults to the showcase API that starting up the docker containers would host
# If you want to override this, make a .env.local file here and specifiy it
VUE_APP_BASE_API=http://localhost:5200
